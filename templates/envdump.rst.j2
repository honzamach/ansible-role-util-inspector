.. _section-envdump:

Full environment dump
================================================================================


.. _section-envdump-hosts:

Host fact dumps
--------------------------------------------------------------------------------

.. toctree::
   :maxdepth: 1
   :glob:

   /inventory/docs/envdump/detail_*


VARIABLES ("vars")
--------------------------------------------------------------------------------

.. code-block:: json

{{ vars | to_nice_json | indent(width=4, first=True, blank=True) }}


ENVIRONMENT variable ("environment")
--------------------------------------------------------------------------------

.. code-block:: json

{{ environment | to_nice_json | indent(width=4, first=True, blank=True) }}


GROUP NAMES variable ("group_names")
--------------------------------------------------------------------------------

.. code-block:: json

{{ group_names | to_nice_json | indent(width=4, first=True, blank=True) }}


GROUPS variable ("groups")
--------------------------------------------------------------------------------

.. code-block:: json

{{ groups | to_nice_json | indent(width=4, first=True, blank=True) }}


HOST variables ("hostvars")
--------------------------------------------------------------------------------

.. code-block:: json

{{ hostvars | to_nice_json | indent(width=4, first=True, blank=True) }}
